<%= form_with(model: audit_entry) do |form| %>
  <% if audit_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(audit_entry.errors.count, "error") %> prohibited this audit_entry from being saved:</h2>

      <ul>
        <% audit_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_area :user %>
  </div>

  <div class="field">
    <%= form.label :resource %>
    <%= form.text_area :resource %>
  </div>

  <div class="field">
    <%= form.label :from %>
    <%= form.text_field :from %>
  </div>

  <div class="field">
    <%= form.label :to %>
    <%= form.text_field :to %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
