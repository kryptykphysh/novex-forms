<%= form_with(model: audit_entry, id: dom_id(audit_entry)) do |form| %>
  <% if audit_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(audit_entry.errors.count, "error") %> prohibited this audit_entry from being saved:</h2>

      <ul>
        <% audit_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user %>
  </div>

  <div class="field">
    <%= form.label :resource %>
    <%= form.text_field :resource %>
  </div>

  <div class="field">
    <%= form.label :old_data %>
    <%= form.text_area :old_data %>
  </div>

  <div class="field">
    <%= form.label :new_data %>
    <%= form.text_area :new_data %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
